{% extends '::base.html.twig' %}

{% block body %}
	
	{% include 'AuditBundle:Audit:menu.html.twig' %}
	
	<br/>
    <div class="table-toolbar">
        <div>
			{% if isAccessibile('crea_metodologia_campionamento') %}
                <a class="btn btn-primary" href="{{ path('crea_metodologia_campionamento') }}">
                    <span class="fa fa-plus"></span> Aggiungi metodologia campionamento
                </a>
			{% endif %}
        </div>
    </div>
    <table class="table table-striped table-hover table-bordered" id="metodologie-campionamento-elenco">
        <thead>
            <tr>
                <th class="text-center">Id</th>
                <th>Denominazione</th>
            </tr>
        </thead>
        <tbody>
			{% for metodologiaCampionamento in metodologieCampionamento %}
				<tr>
                    <td class="table-id">
	                    {{ metodologiaCampionamento.id }}
                    </td>
                    <td>{{ metodologiaCampionamento.denominazione }}</td>
				</tr>
			{% endfor %}
        </tbody>
    </table>

{% endblock %}

